# No environment variables required for standalone mode.
# The app runs fully offline with hardcoded demo data.
#
# TODO (backend adapter): When a backend is ready, add:
# VITE_API_BASE_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000/ws/dashboard
